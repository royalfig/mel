<nav class="navbar container__inner">
    <div class="navbar__left">
        <div class="navbar__logo">
            {{#if @site.icon}}<img src="{{@site.icon}}" alt="{{@site.title}}" width="30" height="30">{{/if}}
            {{^is "home"}}<p>{{@site.title}}</p>{{/is}}
        </div>
    </div>
{{!-- navigation --}}
    <div class="navigation__right">
        {{navigation}}
    </div>
{{!-- buttons --}}
</nav>