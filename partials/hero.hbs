<div class="mel-container">
    <div class="mel-container__inner">
        <div class="mel-hero">
            <div class="mel-hero__left">
                <h1 class="mel-hero__title">Hi, I&apos;m Ryan</h1>
                    <p class="mel-hero__description">I code, create, and teach.</p>
                    <p class="mel-hero__description">
                    It&apos;s usually with some Cincinnati chili and craft beer&nbsp;🍻, beside a little canine called Mel&nbsp;🐩, and in an aspiring MCM ranch&nbsp;🪴.
                    </p>
                    {{^if @member}}
                        <p class="mel-hero__description">
                        Sign up and join us on our coding adventures&nbsp;😍
                        </p>
                        {{> "inline-signup"}}
                    {{else}}
                        <p class="mel-hero__description">
                            Thanks for being a being member&nbsp;😍
                        </p>
                    {{/if}}
            </div>
            <div class="mel-hero__right">
                <img class="mel-hero__img" 
                srcset="{{img_url @site.cover_image size="s"}} 350w, 
                        {{img_url @site.cover_image size="m"}} 500w,
                        {{img_url @site.cover_image size="l"}} 700w,
                        {{img_url @site.cover_image size="xl"}} 1000w,"
                        sizes="(max-width: 768px) 350px, 500px"
                        src="{{img_url @site.cover_image size="m"}}" alt="{{@site.title}}" width="500" height="500">
            </div>
        </div>
    </div>
</div>